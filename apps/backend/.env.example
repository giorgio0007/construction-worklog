# Локальный запуск (backend на хосте, postgres в Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/construction_worklog?schema=public"
PORT=3000

# Внутри Docker DATABASE_URL задаётся в docker-compose.yml:
# postgresql://postgres:postgres@postgres:5432/construction_worklog?schema=public
